import { style } from "@vanilla-extract/css";

export const emptyContainerStyle = style({
  width: "100%",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  flexDirection: "column",
});
export const emptyTextStyle = style({
  fontSize: "2.4rem",
});
